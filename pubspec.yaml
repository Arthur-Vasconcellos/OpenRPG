name: openrpg
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  drift: ^2.28.1
  file_picker: ^10.3.2
  path_provider: ^2.1.5
  path: ^1.9.1
  share_plus: ^12.0.1
  sqlite3_flutter_libs: ^0.5.39

dev_dependencies:
  build_runner: ^2.5.4
  drift_dev: ^2.28.2
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/magic_items.json
    - assets/rulesets/
